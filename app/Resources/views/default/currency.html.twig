<ul class="list-group list-group-flush">
    {% for item in items %}
        <li class="list-group-item"><strong>{{ item.name }}:</strong> {{ item.value }} TL ({{ item.provider }})</li>
    {% else %}
    <li class="list-group-item">No data!</li>
    {% endfor %}
</ul>